// The popup only renders when the extension is installed and enabled, so its status is simply "Active".
// No tab queries or extra permissions needed — Otto detects the extension in-page via the data-weave-ext flag.
