node_modules
dist
.env
*.local
public/otto-tabs-extension.zip
# Generated by scripts/build-vercel.sh (esbuild-bundled server/index.ts) on every build — was previously
# committed and could go stale relative to source (observed: an old committed copy still imported the
# pre-fork "pawnote" package name after the real source had switched to "@blockshub/pawnote-lts").
api/index.mjs
