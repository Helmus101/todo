# Base44 development defaults — placeholders so the app boots without credentials.
# Real secrets (DEEPSEEK_API_KEY, COMPOSIO_API_KEY, SUPABASE_*, etc.) are delivered via
# /run/base44/app.env and override these (compose env_file order: this file FIRST, app.env LAST).

# Non-secret dev defaults
DEEPSEEK_MODEL=deepseek-v4-flash
MONTHLY_AI_BUDGET_USD=3
PLAID_MOCK=1
