# Development defaults — real secrets override these via /run/base44/app.env (last env_file entry wins).
# No env vars are required to boot in dev mode (see AGENTS.md). These placeholders just keep the app happy.
SESSION_SECRET=dev-session-secret-not-for-production
DEEPSEEK_MODEL=deepseek-v4-flash
MONTHLY_AI_BUDGET_USD=100
PUBLIC_URL=http://localhost:5273
